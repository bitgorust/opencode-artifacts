# Evidence: Expose complete artifact lifecycle operations

Evidence is pending approval and implementation. Archive validation will add one section for
each affected requirement with exact test/model/manual links, CLI/plugin journeys, hostile
reference and bundle results, compatibility observations, and visible failures or exclusions.
