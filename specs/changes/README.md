# Active changes

Each child directory is a standard or high-risk change packet created with `npm run spec`.
Only active packets live here. Verified packets and explicitly withdrawn proposals move to
[`../archive/`](../archive/).

Do not use an active packet as the current behavior contract. Until it is implemented,
verified, reflected in [`../current/`](../current/), and archived, it remains a proposed
delta.
