# Archived changes

Verified packets are stored as `<YYYY-MM-DD>-<change-id>`. They preserve decision and evidence
history but do not override [`../current/`](../current/) or the target product specification.

Archive through `npm run spec -- archive <change-id>` so the final gates run before the move.
